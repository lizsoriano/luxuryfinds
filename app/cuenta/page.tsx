import { Badge } from "../../components/ui/Badge"; import { Button } from "../../components/ui/Button"; import { Card } from "../../components/ui/Card"; import { PageHeader } from "../../components/ui/PageHeader"; import { StatCard } from "../../components/ui/StatCard"; import { mockAccountPurchases } from "../../lib/mock-data";
export default function AccountPage(){return <main className="account-content"><PageHeader eyebrow="JUEVES, 27 DE AGOSTO" title={<>Hola, Andrea <em>♡</em></>} description="Aquí tienes un resumen de tus compras y próximos pasos."/><section className="account-stats"><StatCard label="TU PRÓXIMO PAGO" value="$525" note="Lunes 31 de agosto" tone="rose"/><StatCard label="COMPRAS ACTIVAS" value="2" note="Una lista para entrega"/><StatCard label="NOTIFICACIONES" value="3" note="Tienes una nueva hoy"/></section><section className="account-grid"><div><div className="section-heading"><div><p className="micro-label">MIS COMPRAS</p><h2>En movimiento</h2></div><Button href="/cuenta/compras" variant="secondary" size="small">Ver todas</Button></div><div className="purchase-list">{mockAccountPurchases.map((purchase,index)=><Card className="purchase-row" key={purchase.ticket}><div className={`purchase-thumb tone-${index ? "rose" : "cream"}`}>LF</div><div className="purchase-main"><small>{purchase.ticket}</small><h3>{purchase.name}</h3><p>{purchase.detail}</p></div><div className="purchase-state"><Badge tone={index ? "success" : "rose"}>{purchase.state}</Badge><strong>{purchase.amount}</strong></div></Card>)}</div></div><Card className="account-side-card"><p className="micro-label">PROGRESO DEL PLAN</p><h2>Ya falta poco.</h2><div className="progress-ring"><span>63<small>%</small></span></div><p>Has completado 5 de 8 pagos de tu plan.</p><div className="mini-progress"><span/></div><Button href="/cuenta/pagos" variant="secondary" fullWidth>Ver mis pagos</Button></Card></section></main>}
