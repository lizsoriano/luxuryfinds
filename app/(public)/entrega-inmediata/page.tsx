import { ProductCard } from "../../../components/ui/ProductCard"; import { PageHeader } from "../../../components/ui/PageHeader"; import { mockProducts } from "../../../lib/mock-data";
export default function ImmediatePage(){const products=mockProducts.filter((p)=>p.availability==="Entrega inmediata");return <main className="catalog-page"><div className="shell"><PageHeader eyebrow="LISTO PARA TI" title={<>Entrega <em>inmediata.</em></>} description="Productos disponibles físicamente en La Paz."/><div className="product-grid">{products.map((product)=><ProductCard product={product} key={product.id}/>)}</div></div></main>}
