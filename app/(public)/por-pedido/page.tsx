import { ProductCard } from "../../../components/ui/ProductCard"; import { PageHeader } from "../../../components/ui/PageHeader"; import { mockProducts } from "../../../lib/mock-data";
export default function OnDemandPage(){const products=mockProducts.filter((p)=>p.availability==="Por pedido");return <main className="catalog-page"><div className="shell"><PageHeader eyebrow="LO ENCONTRAMOS PARA TI" title={<>Compras <em>por pedido.</em></>} description="Elige una pieza especial y acompaña su avance desde tu cuenta."/><div className="product-grid">{products.map((product)=><ProductCard product={product} key={product.id}/>)}</div></div></main>}
